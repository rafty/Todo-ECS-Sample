# Tasks: 003-agentsmd_refactoring

## 前提確認
- [x] ルート `AGENTS.md` と `infra/AGENTS.md` を再確認し、変更範囲を `infra/` に限定する前提を明確化する。
- [x] `infra/README.md` の主要コマンド（`build` / `test` / `synth` / `diff`）と環境切替ルール（`-c env=<dev|stg|prod>`）を確認する。
- [x] `infra/specs/003-agentsmd_refactoring/spec.md` の機能要件・非機能要件・受け入れ条件を確認し、逸脱しない作業境界を定義する。
- [x] `infra/specs/003-agentsmd_refactoring/plan.md` の「変更対象 / 変更しないもの / 未解決事項」を確認し、今回の実装対象を `infra/lib/` 中心に確定する。
- [x] `infra/specs/002-create_network/` の仕様を参照し、ネットワーク要件（環境切替・タグ運用）を破壊しない制約を整理する。

## 実装タスク
### 1. 現状把握・不整合抽出
- [x] `infra/lib/` の既存ネットワーク関連 Stack / Construct を棚卸しし、責務過多・再利用性不足・可読性低下の観点で不整合候補を列挙する。
- [x] 不整合候補を「Stack責務」「Construct分割」「命名/配置」「環境切替」「タグ運用」「高リスク変更（IAM/Network/SG）」に分類し、優先度を付ける。
- [x] 今回対応しない事項（新規AWSサービス追加、広範囲rename、本番影響設定変更）を除外リストとして明示する。

### 2. 設計確定（最小変更方針）
- [x] Stack の単一責務を満たすため、分割が必要な箇所と据え置く箇所を決定する。
- [x] `infra/lib/constructs/` へ抽出する Construct の候補を定義し、各 Construct の責務（何をまとめるか）を1つに限定する。
- [x] 既存の `context` / `config` / `props` の受け渡し方式を維持し、環境切替ロジックを変更しない設計を確定する。
- [x] L2/L3 Construct 優先方針に照らし、L1 利用の要否を判定する（必要な場合のみ理由をコメント方針に沿って記録する）。
- [x] 論理ID変化やリソース置換のリスクが高い箇所を事前にマーキングし、実装時の注意点を整理する。

### 3. 実装修正（必要最小限）
- [x] 事前設計に基づき `infra/lib/` の対象 Stack を最小差分で修正し、責務境界を明確化する。
- [x] 必要な場合のみ `infra/lib/constructs/` に Construct を追加し、再利用可能なインフラ構成単位へ切り出す。
- [x] 既存の環境指定（`-c env=<...>`）およびタグ方針（`env` / `service` / `version`）に互換性があることをコード上で担保する。
- [x] IAM / ネットワーク / Security Group に関わる差分は、変更理由と最小権限方針が説明可能な状態にする。
- [x] 無関係な整形・命名変更・ファイル移動を避け、レビュー可能な差分粒度を維持する。

## テスト / 検証タスク
- [x] `infra/` で `npm run build` を実行し、TypeScriptコンパイル整合性を確認する。
- [x] `infra/` で `npm test -- --runInBand` を実行し、既存テストの回帰を確認する。
- [x] `infra/` で `npx cdk synth -c env=prod` を実行し、テンプレート生成が成功することを確認する（必須）。
- [x] 影響が大きい差分（論理ID変化、ネットワーク/IAM/SG関連）を含む場合は `npx cdk diff -c env=prod` を実行し、置換・削除の有無を確認する。
- [x] 実施できない検証がある場合は、未実施理由・未確認範囲・残リスクを記録する。

## ドキュメント更新タスク
- [x] `infra/README.md` の更新要否を判定する（主要コマンド、構成説明、環境切替、タグ運用に変更がある場合のみ更新）。
- [x] `docs/infra/` の更新要否を判定する（ネットワーク構成や運用フローの説明変更がある場合のみ更新）。
- [x] 設計判断の恒久的変更がある場合は `docs/adr/` の追加・更新要否を判定する。
- [x] ドキュメント更新を行う場合は、すべて日本語で記述し、必要に応じて Mermaid 図の追加を検討する。

## 完了確認
- [x] `spec.md` の受け入れ条件（範囲限定、責務明確化、検証結果提示、ドキュメント更新要否確認）を満たしていることを確認する。
- [x] 変更が `infra/` の依頼範囲に収まり、対象外変更（新規サービス追加、横断変更）が混入していないことを確認する。
- [x] 既存の環境切替・タグ運用・コマンド運用との整合を最終確認する。
- [x] 差分にシークレット、認証情報、stateファイル、不要ファイルが含まれていないことを確認する。
- [x] 検証結果と未実施事項（あれば）を明確に記録し、レビュー可能な完了状態にする。

---

並列化の目安:
- 「1. 現状把握・不整合抽出」は先行必須。
- 「2. 設計確定」完了後に「3. 実装修正」へ進む。
- 「テスト / 検証タスク」は実装修正後に実施。
- 「ドキュメント更新タスク」は検証結果を踏まえて並行可能。

実施メモ:
- `npm run build`: 成功。
- `npm test -- --runInBand`: 成功（`ts-jest` の既知警告 TS151002 のみ）。
- `npx cdk synth -c env=prod`: AWS認証エラー（`Could not assume role... self-signed certificate in certificate chain`）で環境要因により未完了。
- `npx cdk diff -c env=prod`: 上記と同一のAWS認証エラーで環境要因により未完了。
- 未確認範囲: 実AWSアカウントに対する `synth/diff` の最終差分確認。残リスク: 論理ID変化起因の置換有無は認証復旧後に再確認が必要。